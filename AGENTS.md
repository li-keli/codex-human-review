# 仓库维护规则

## 范围与语言

本仓库只维护通用 Codex 人工复核插件，必须完全独立于任何公司和具体业务。说明、审核结论和提交信息使用简体中文。保持改动范围最小。

## 每次提交必须进行脱敏检查

每次 commit、push、创建 PR 或发布版本之前，必须检查完整暂存内容、待推送的全部提交及提交元数据，不能只检查最近编辑的文件。首次提交必须审核全部待跟踪文件。

禁止提交任何公司内部信息或内容，包括内部源码、业务规则、需求与架构资料、内部项目名称、域名和地址、接口与数据库结构、工单、文档、聊天、日志、截图以及真实数据；即使删除名称或标注为示例也不能携带其实际内容。

禁止提交账号凭据、访问令牌、Cookie、私钥、环境配置、连接文件、审核会话状态、真实问题和回答、真实任务 ID、个人绝对路径、联系方式以及其他可识别信息。提交作者不得使用公司邮箱，优先使用 GitHub noreply 邮箱。

所有示例、测试夹具和文档内容必须从零编写为通用虚构场景，与公司及业务完全无关；优先使用配色、字号等界面偏好。禁止从内部材料复制后仅替换名称。

提交前必须：

1. 核对 `git status --short`、`git diff --cached --stat` 和完整 `git diff --cached`；推送前检查尚未推送的全部提交。
2. 检查新增文件、删除内容、二进制、生成物、提交说明、作者姓名与邮箱；搜索密钥、真实地址和路径等风险线索。自动扫描不能代替人工逐文件审核。
3. 确认运行状态、临时产物和本机配置未被跟踪，检查 `.gitignore`。`.gitignore` 不会保护已经跟踪的文件。
4. 发现不确定内容时先移出提交范围，完成核查后再提交。不能因为仓库是私有的就跳过检查。
5. 运行与改动相关的测试，并在交付说明中记录脱敏审核范围和测试结果；审核记录本身也不得包含被移除的敏感原文。

如果敏感内容已进入本地历史，停止推送，先清理历史；如已推送，停止继续传播并通知仓库所有者处理，不将删除工作区文件视为已完成清理。

## 临时文件与测试

中间产物必须放在 `$TMPDIR` 下的本次任务独立目录。禁止在仓库中保存实际审核会话或复制真实用户资料。测试仅使用虚构数据，不能替用户提交实际决定。

## Git 与验证

禁止从 `live` 分支开发，禁止将其内容合入开发或主分支。新功能分支使用 `codex/` 前缀。

提交说明使用 `type(review): 中文说明`，说明改动、原因、影响和验证结果。

常用验证：

```sh
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v
node --test tests/frontend.test.cjs
node --check web/app.js
```
